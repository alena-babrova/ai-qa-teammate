**Headless CI.** Target **`__ISSUE_KEY__`**; story **`__STORY_KEY__`** (workflow-resolved). If **`__ISSUE_KEY__` ≠ `__STORY_KEY__`**, the Sub-task is **trigger only**—requirements, paths, and Test links use **`__STORY_KEY__`** only.

1. **Verify you can read the story (Jira MCP).** Before any test work, **`jira_get_issue(__STORY_KEY__)`** / **`jira_getIssue(__STORY_KEY__)`** must **succeed** with non-empty Jira **`summary`** and **`description`** present on the issue (body may be empty). If MCP is missing or the call fails: **do not** write **`generated/jira-tests/__STORY_KEY__/tests.json`** or **`meta.json`**—let verify fail. Full gate, empty story, **`tests.json`** / **`meta.json`**, MCP-only writes: **`.cursor/rules/jira-test-issues.mdc`**.

2. **Requirement signals (CI).** Read **`generated/jira-tests/__STORY_KEY__/requirement-signals.json`** when present (written by **`scripts/extract-requirement-signals.js`** before this run). Use **`gaCoverageRequired`**, **`gaHints`**, and **`sources`** to plan coverage. If **`requirementsReadFailed`** is true, the workflow should already have failed—do not proceed.

3. **Linked specs (optional MCP reads).** If the story **`description`**, AC/DoD-style fields, or (when fetched) **comments** contain **Confluence**, **Figma**, or **EPAM GitLab** (**`https://git.epam.com/`**) URLs, use the **Confluence** / **Figma** / **GitLab** MCP tools to **read** that linked content **before** deriving tests—**minimize tool calls** (dedupe URLs; one read per distinct page/file; prefer single broad Figma file reads over many small calls; **no** Figma browsing beyond linked URLs; **no** GitLab repo crawling beyond URLs linked from the story). After a successful GitLab/Confluence read, also fetch **nested** URLs cited as authoritative for **Analytics (GA4)** (**`jira-test-issues.mdc`** → **Nested authoritative links**). If there are no such links, skip. If a read fails, continue from Jira text and **`requirement-signals.json`** when available—**do not** narrate errors, HTTP codes (e.g. 429), or “continuing from Jira only” in streamed output—see **`jira-test-issues.mdc`** → **Story-linked Confluence, Figma, and GitLab**.

4. **Generate tests.** Use **only** that story payload (never Sub-task body when keys differ), **plus** any linked Confluence / Figma / GitLab content you successfully fetched in step 3, **plus** **`requirement-signals.json`** when present. **First** discover **Test** issues already **linked** to **`__STORY_KEY__`** (MCP); **do not** duplicate—**update** matching Tests (**`jira_update_issue`**) and **`jira_create_issue`** only for missing cases (**`jira-test-issues.mdc`** → **Sync with existing Jira Tests**). Emit **`generated/jira-tests/__STORY_KEY__/tests.json`**, sync Jira Tests via MCP, and **`meta.json`** with **`mcpCreatedKeys`** in **`tests[]`** order (keys for updated **or** new issues). When **creating** or **updating** Tests, set **Folder** **`customfield_24001`** to the TM **`AI Generated`** option (on the Jira instance, the **Folder** option label is often **`AI Generated `** with a trailing space—confirm with **`jira_get_issue`** / **`jira_get_field_options`** for **`customfield_24001`** when unsure; not the project-root folder **`EPMRPP`**; full rules in **`jira-test-issues.mdc`**), **Test Library** **`customfield_24000`**, **Test Requirement** **`customfield_29300`** to **`__STORY_KEY__`** (mirror the value shape Jira returns on a Test that already has **Test requirement** set—see **`jira-test-issues.mdc`** / **MCP create / update**), and **`priority`** per **Test priority** in **`jira-test-issues.mdc`**—**do not** default every Test to **Major**. When **linking** each Test to **`__STORY_KEY__`**, use link type **`QaSpace test`** (**id `12700`**): **`inwardIssue` = Story**, **`outwardIssue` = Test** so the story shows **is tested by**—**`jira-test-issues.mdc`** → **Linking Tests to the Story**. **Do not** comment on **`__STORY_KEY__`**. After sync, if **`__ISSUE_KEY__` ≠ `__STORY_KEY__`**, add **one** completion comment on **`__ISSUE_KEY__`** (Test Design Sub-task) only: **summary + list of Test keys/titles**—**no** gaps or limitations (**`jira-test-issues.mdc`** → **Test Design sub-task comment after successful test generation**). If **`__ISSUE_KEY__` = `__STORY_KEY__`**, **omit** that comment.

5. **GA coverage (when required).** If **`requirement-signals.json`** has **`gaCoverageRequired`: true**, emit at least **two** **`tests[]`** rows whose **`summary`** starts with **`GA.`** (Analytics ON + **Set GA OFF** / **GA OFF** pair unless the spec defines a single event only). When GA4 KB or spec lists **`place`**, **`category`**, **`type`**, or event numbers, **`expectedResult`** / steps must match those values—not invented placeholders. Respect **out of scope** in the spec (do not add GA for actions owned by other stories).

6. **Format and shape.** Style and wording: **`.cursor/rules/jira-test-cases-epmrpp-style.mdc`**. **Before authoring**, read **`test-case-examples/CONTEXT.md`** and consult matching files in **`test-case-examples/`** (see **`.cursor/skills/jira-story-test-cases-md/SKILL.md`**). **Very optional:** when deciding **which** cases to write, search the related **Epic** for **Closed** sibling stories and skim **`Test`** issues linked to the closest matches; if still unclear, sample the Jira **`Test`** library via targeted JQL (**skill** → **Investigating coverage in Jira**); skip if not needed or if MCP fails. **`tests[].summary`** / Jira Test **Summary** = EPMRPP title **without** **`__STORY_KEY__`** (or Sub-task **`__ISSUE_KEY__`** when it differs). How fields map to **`tests[]`** / Jira: **`.cursor/skills/jira-story-test-cases-md/SKILL.md`**.

No questions; no secrets in repo files.

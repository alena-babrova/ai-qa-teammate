**Headless CI.** Target **`__ISSUE_KEY__`**; story **`__STORY_KEY__`** (workflow-resolved). If **`__ISSUE_KEY__` ≠ `__STORY_KEY__`**, the Sub-task is **trigger only**—requirements, paths, and Test links use **`__STORY_KEY__`** only.

1. **Verify you can read the story (Jira MCP).** Before any test work, **`jira_get_issue(__STORY_KEY__)`** / **`jira_getIssue(__STORY_KEY__)`** must **succeed** with non-empty Jira **`summary`** and **`description`** present on the issue (body may be empty). If MCP is missing or the call fails: **do not** write **`generated/jira-tests/__STORY_KEY__/tests.json`** or **`meta.json`**—let verify fail. Full gate, empty story, **`tests.json`** / **`meta.json`**, MCP-only writes: **`.cursor/rules/jira-test-issues.mdc`**.

2. **Linked specs (optional MCP reads).** If the story **`description`**, AC/DoD-style fields, or (when fetched) **comments** contain **Confluence** or **Figma** URLs, use the **Confluence** / **Figma** MCP tools to **read** that linked content **before** deriving tests—**minimize tool calls** (dedupe URLs, one read per distinct page/file, prefer single broad Figma file reads over many small calls; **no** Figma browsing beyond linked URLs). If there are no such links, skip. If a read fails, continue from Jira text only—**do not** narrate errors, HTTP codes (e.g. 429), or “continuing from Jira only” in streamed output—see **`jira-test-issues.mdc`** → **Story-linked Confluence and Figma**.

3. **Generate tests.** Use **only** that story payload (never Sub-task body when keys differ), **plus** any linked Confluence/Figma content you successfully fetched in step 2. **First** discover **Test** issues already **linked** to **`__STORY_KEY__`** (MCP); **do not** duplicate—**update** matching Tests (**`jira_update_issue`**) and **`jira_create_issue`** only for missing cases (**`jira-test-issues.mdc`** → **Sync with existing Jira Tests**). Emit **`generated/jira-tests/__STORY_KEY__/tests.json`**, sync Jira Tests via MCP, and **`meta.json`** with **`mcpCreatedKeys`** in **`tests[]`** order (keys for updated **or** new issues). When **creating** Tests, set **Folder** **`customfield_24001`** under **`AI Generated / __STORY_KEY__`** (not EPMRPP root only) and **Test Library** **`customfield_24000`** per **`jira-test-issues.mdc`**. After sync, comment on **`__STORY_KEY__`**: **summary + list of Test keys/titles only**—**no** gaps or limitations (**`jira-test-issues.mdc`** → **Story comment after successful test generation**).

4. **Format and shape.** Style and wording: **`.cursor/rules/jira-test-cases-epmrpp-style.mdc`**. **`tests[].summary`** / Jira Test **Summary** = EPMRPP title **without** **`__STORY_KEY__`** (or Sub-task **`__ISSUE_KEY__`** when it differs). How fields map to **`tests[]`** / Jira: **`.cursor/skills/jira-story-test-cases-md/SKILL.md`**.

No questions; no secrets in repo files.

**GitHub Actions (headless).** Workflow target: **`__ISSUE_KEY__`**. Story/source for test generation and **`generated/jira-tests/...` paths: **`__STORY_KEY__`** (same as target, **or** the **parent** when the target is a **Sub-task**).

1. Follow **`.cursor/rules/ai-test-generator.mdc`** (canonical schema, no interactive questions) and **`.cursor/rules/jira-test-create.mdc`** (project, **Test** type, Test Steps / Expected custom fields).
2. Follow **`.cursor/skills/jira-story-test-cases-md/SKILL.md`**, especially **`## Headless CI (this repository, GitHub Actions)`**, for how to derive tests from the story.
3. **Task:** Use **`__STORY_KEY__`** as the **story** for test generation and output paths (in CI it is already the parent when the workflow target **`__ISSUE_KEY__`** was a **Sub-task**). Load **`__STORY_KEY__`** with Jira (`jira_getIssue`, etc.) and base cases on that issue’s description, AC, and scope—not on a Sub-task’s summary alone. If you only see **`__ISSUE_KEY__`**, load it first: when **issue type** is **Sub-task** (or equivalent), use **`fields.parent.key`** as the story key and follow the same rules. Write **`generated/jira-tests/__STORY_KEY__/tests.json`** and **`meta.json`**. Create **every** Test issue, link to **`__STORY_KEY__`**, **via MCP**; then add required **`meta.json`** (`jiraPublish`, `mcpCreatedKeys`) per the rules.
4. Do **not** ask questions; do **not** put secrets in files.
